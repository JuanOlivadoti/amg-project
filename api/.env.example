# NO edites `api/.env` a mano: lo GENERA `npm run env:sync` desde docs/private/credenciales.env.
# Este archivo es el CONTRATO del paquete: scripts/env-sync.test.mts verifica que estas claves
# coincidan EXACTAMENTE con el MAPA que decide quien recibe que.
#
# api/.env — plantilla de las variables de la API (portal backend).
#
# En LOCAL: no hace falta ninguna — `npm run dev:server -w api` corre sobre PGlite con un verificador
# de token falso, sin credenciales. Estas variables son para el `npm run serve -w api` de PRODUCCIÓN
# (Railway), que falla cerrado si falta alguna de las obligatorias.
#
# En Railway NO se usa un archivo .env: estas mismas claves se cargan como "Variables" del servicio.
# Esta plantilla es la lista de qué poner. Los valores (secretos) NUNCA van al repo ni al chat.

# ── Obligatorias (sin ellas la API no arranca) ──────────────────────────────────

# Conexión del login `amg_api` (rol app_user, RLS). NO es la de admin de las migraciones.
# Sale de Supabase → Project Settings → Database, y le ponés la contraseña que fijaste con
# `alter role amg_api with password ...` (ver docs/proyecto/12-credenciales.md).
DATABASE_URL_API=postgres://amg_api:LA-PASSWORD@HOST:5432/postgres

# `iss` del proyecto Supabase. La API deriva de acá el JWKS
# (`<iss>/.well-known/jwks.json`) y verifica con él la firma ES256 del token.
# NO hay secreto compartido: la clave privada nunca sale de Supabase, y de acá
# sale la pública. Además amarra el emisor: un token válido de OTRO proyecto no entra.
# Tiene que ser exactamente https://<project-ref>.supabase.co/auth/v1 (sin barra final).
# El <project-ref> sale de Supabase → Project Settings → General → Reference ID.
SUPABASE_JWT_ISS=https://<project-ref>.supabase.co/auth/v1

# Origen(es) del portal, separados por coma. NO se acepta `*` (la API lo rechaza explícitamente:
# ver `esOrigenHttp` en deps.ts). http:// SÍ se acepta —es lo que usa el desarrollo local
# (http://localhost:4200)— pero en PRODUCCIÓN el origen tiene que ser https.
# El portal vive en https://bigballs.es (Hostinger). Si también servís www, agregá el segundo:
#   CORS_ORIGINS=https://bigballs.es,https://www.bigballs.es
CORS_ORIGINS=https://bigballs.es

# Clave de ESCRITURA de Inngest: con ella la API emite `research/solicitado`.
# Inngest → el entorno → Event Keys. **Obligatoria si el SDK está en modo cloud**: sin ella `send()`
# lanza, y como la fila del run se crea ANTES de emitir (ADR-18), cada intento dejaba un run huérfano
# en `running`. Fue exactamente el fallo de producción del 2026-08-07.
# ⚠️ NO es la `INNGEST_SIGNING_KEY`: ésta autentica el ENVÍO y la otra verifica la RECEPCIÓN. La de
# recepción es del orquestador y la API no la tiene — son dos procesos y dos mitades distintas.
INNGEST_EVENT_KEY=

# Secreto con el que se firma/verifica el `state` del callback OAuth de Google
# (api/src/oauth-state.ts). `GET /clients/:id/google/callback` lo pega una navegación real del
# navegador -- nunca lleva el header Authorization -- así que esta es la ÚNICA forma de que ese
# callback anónimo confíe en el tenantId/userId de quien conectó, en vez de aceptar cualquier valor
# que alguien le mande a mano en la URL. Generalo con `npm run credencial -- OAUTH_STATE_SECRET`.
OAUTH_STATE_SECRET=

# ── Opcionales ───────────────────────────────────────────────────────────────────

# `aud` esperado. Por defecto `authenticated` (lo que emite Supabase). Dejalo vacío si no cambiaste el default.
SUPABASE_JWT_AUD=

# `mock` | `live`. Opcional, con default `mock`: en qué modo el flujo de "Conectar Google" arma el
# proveedor OAuth (`api/src/google-oauth.ts`). Mismo botón de operación que lee el orquestador para
# el polling (ver `orchestrator/.env.example`) -- son dos procesos separados y cada uno necesita su
# propia variable, pero tienen que declarar lo mismo. Si la ponés, se valida: un typo no cae en
# silencio a `mock`, la API falla al arrancar.
GOOGLE_REVIEWS_MODO=

# ── La inyecta Railway sola ──────────────────────────────────────────────────────
# PORT — no la pongas a mano; Railway la asigna y el server la lee.
