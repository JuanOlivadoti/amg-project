# Dependencias
node_modules/
/yarn.lock
# El package-lock.json de la RAÍZ sí va al repo: con workspaces es el único lockfile y es lo que
# hace reproducible la instalación. Los de los paquetes no deben existir.
*/package-lock.json

# Builds / salidas
dist/
out/
/out-tsc/
*.tsbuildinfo

# Angular
/.angular/
.angular/
/.ng/
/coverage/
/e2e/test-output/

# Variables de entorno (secretos)
.env
.env.local
*.env

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos temporales de Office
~$*

# Sistema
.DS_Store
Thumbs.db

# Cache de respuestas de DataForSEO (datos pagos, pero regenerables; no van al repo)
.cache/
